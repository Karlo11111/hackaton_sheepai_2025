import 'package:flutter/material.dart';

class SanboxPage extends StatelessWidget {
  const SanboxPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Work in progress'),),);
  }
}